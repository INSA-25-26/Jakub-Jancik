{
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	encode gzip

	redir /swagger /docs 308
	redir /healthz /health 308

	handle /grafana* {
		reverse_proxy grafana:3000
	}

	handle /prometheus* {
		reverse_proxy prometheus:9090
	}

	handle {
		reverse_proxy app:8000
	}
}
